


<nav class="navbar navbar-expand-lg bg-success position-sticky ">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">MovieMania</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarScroll">
          <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
            <li class="nav-item">
              <a class="nav-link text-light  mx-2" aria-current="page" href="/movies">Home</a>
            </li>
            <% if(currentUser &&  currentUser.role === 'contributer'){ %>
              <li class="nav-item">
                <a class="nav-link text-light  mx-2" href="/movies/new">New</a>
              </li>
            <% } %>
          </ul>
          <div class="d-flex text-light" role="search">
              <% if(!currentUser){ %>
                <a class="nav-link mx-2 btn btn-info p-1" href="/singup"><i class="fa-solid fa-user-plus"></i>SingUp</a>
                <a class="nav-link mx-2 btn btn-info p-1" href="/login"><i class="fa-solid fa-right-to-bracket"></i>Login</a>
              <% }else { %>
                <a href="/user/cart" class="nav-link mx-2"> 
                  <i class="fa-solid fa-cart-plus"></i>
                  <sup><span class="badge bg-danger "><%= currentUser.cart.length %></span></sup>
                </a>
                <a class="nav-link mx-1 btn btn-info px-1" href="/logout">Logout</a>
                <a class="nav-link mx-2 " href="#">
                  <img style="width: 25px;border-radius: 50%; height: 25px;" class="mx-1 mt-1" src="<%= currentUser.img %>" alt="profileImage">
                  <%= currentUser.username %>
                </a>
              <% } %>
          </div>
      </div>
    </div>
</nav>







